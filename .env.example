# Database (PostgreSQL — Neon or any Postgres instance)
DATABASE_URL=

# NextAuth v5 — generate with: openssl rand -base64 32
# (NEXTAUTH_SECRET also works as a fallback — some deployments still use the v4 name)
AUTH_SECRET=
AUTH_URL=

# Razorpay (payments)
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=
NEXT_PUBLIC_RAZORPAY_KEY_ID=

# Cloudinary (image uploads)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Fast2SMS (OTP delivery)
FAST2SMS_API_KEY=

# Resend (transactional email)
RESEND_API_KEY=
FROM_EMAIL=

# Google Indexing API (optional — pings Google when a listing goes live)
GOOGLE_CLIENT_EMAIL=
GOOGLE_PRIVATE_KEY=

# Upstash Redis (optional — enables OTP rate limiting; skipped if unset)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Public app URL (used for canonical links, sitemaps, indexing pings)
NEXT_PUBLIC_APP_URL=https://pgsathi.in

# 1. Admin Portal Test (User/Owner tab se login karein)
#
# Phone: 9999999991
# Password: password123
# 2. Owner Portal Test (User/Owner tab se login karein)
#
# Phone: 9999999992
# Password: password123
# 3. Tenant (Normal User) Portal Test (User/Owner tab se login karein)
#
# Phone: 9999999993
# Password: password123
# 4. Manager Portal Test (Manager tab select karein)
#
# Email: manager_dummy@pgsathi.in (Note: Manager login sirf email support karta hai)
# Password: password123